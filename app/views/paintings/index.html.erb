<div class="main">

	<h1>Awesome Famous Paintings</h1>

	<% @paintings.each do |p| %>


		<div class="text-center">
			<%=p.name%>
		</div>
		<div class="details">
			<span class="glyphicon glyphicon-tint">
			Painted by <%=p.artist%> in <%=p.year %>.</span>
		</div>

	<% end %>

</div>
